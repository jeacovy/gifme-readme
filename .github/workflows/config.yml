# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be ecxecuted as part of the job
    steps:
      - uses: actions/checkout@v2
      # Runs a single command using the runners shell
      - name: Run Readme Gifme Script
        env:
          GIPHY_API_KEY_DEV: '${{ secrets.GIPHY_API_KEY_DEV }}'
        run: sh ./readme-gifme.sh '${GIPHY_API_KEY_DEV}'
      - name: Update changes in GitHub repository
        if: steps.git-check.outputs.modified == 'true'
        run:  |
          git config --global user.name '${{ secrets.GIT_USER }}'
          git config --global user.email ${{ secrets.GIT_EMAIL }}'
          git add -A
          git commit -m '👾 is it pronounce Gif or Gif? [automated commit]'
          git push  